{
  "name": "node-and-express",
  "version": "1.0.0",
  "description": "a simple RESTful API web server with express",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js",
"build:ui":"rm -rf dist && cd ../fullstackopen-lectureparts && npm run build && cp -r dist ../fullstackopen-part3",
"deploy:full":"npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
